export const suma = (x = 0, y = 0) => x + y;

export const multi = (x, y) => x * y;

const titolI = "Hola a tothom";
const actiuI = true;
const punts = [10, 20, 30];

export { titolI, actiuI, punts };

export default suma;
