/*

const promesa = new Promise((resolve, reject) => {

	setTimeout(() => {

		const exit = true;
		if(exit) {
			resolve();
		
		}else{
			reject();
		}

	}, 2000);

});

promesa.then(() => alert ("èxit"));

promesa.catch(() => alert ("fracàs"));


const promesa2 = new Promise((resolve, reject) => {

	setTimeout(() => {

		const exit = true;
		if(exit) {
			resolve("Hem triomfat");
		
		}else{
			reject("Hem fracassat");
		}

	}, 4000);

});

promesa2.then((missatge) => {
	alert (missatge);
});

promesa2.catch((missatge) => {
	alert (missatge);
}); */
"use strict";